# Revision42 software validation and Run40 status

Revision42 / `collision_quality_final_09` passed **2,709 full ROS tests with zero failures, errors or skips**, plus actual selected/default four-node constructor checks. The 895 official-file checks, 40 pinned URDF meshes and source identity checks passed. The [exact portable certificate](evidence/revision42/software_certificate.json) pins all 272 files, source tree `f23d05216ac2abd66a41e08a5ae176f9b7ed1e9096dcb9a43cd065aacdbf34df`, and unchanged selected profile `590bc458…`. Its source was promoted separately after validation; the certificate itself does not claim a physical result or authorize dispatch. Independent read-only review passed all 36 checks, including actual JUnit, constructors, the 272-file package and full validation source/evidence inventories. Official-file checking means 893 exact files plus two permitted bytecode-header refreshes with unchanged payloads. The review hashes are retained in the portable summary.

**Run40 completed the physical sequence on these exact revision42 bytes; its result and limitations are recorded below.** The earlier completed delivery is [Run39 on revision41/final08](revision41_validation.md#completed-run39-physical-result). Run39's 0.474768 mm side margin, 17.254 mm /20.742 degree planning-hold drift, nominal-envelope mismatch and 40 min 32.731 s observer wall time remain material limits. Its sampled screen/table/bin evidence is not continuous clearance, a gentle-placement score, competition readiness or evidence for subsequent source bytes.

## Two bounded controller changes

Ordinary retained PLACE sends now share the existing contact-stop command lock and recheck cancellation and the full watchdog before publication. The sensor lock encloses the final cancel check, pending-token registration and send. Locks are released before waiting for acceptance or a result; existing uncertain-send and cancellation handling remain. The 13 new regression cases cover selected interleavings and error paths, not every possible executor race or physical cargo motion.

Scene checks now reuse independently lazy world-array minimum and maximum reductions only within one sample. Eligible arrays are private ordinary float64 outputs from the existing transforms/concatenation; custom or aliased provenance retains original reductions. The original transformed vertices, predicate calls, rejection order, sampling and exact arithmetic remain. There is no new cross-pose verdict cache. The 28 new differential cases cover ordered predicate arguments, rejections, custom-input fallbacks and ownership; the full suite includes both new test groups.

The [portable change/replay summary](evidence/revision42/controller_replay_summary.json) pins the reviewed sources and local evidence. Grasp settings, geometry predicates, route/search limits, selected profile, official assets/controllers and physics are unchanged. These changes do not measure or correct the book's drift relative to the nominal attachment.

## Bounded nominal replay

One ordered, isolated A/B pair compared exact final08 with only the AABB helper changed. Both completed the full Run38 logged-input nominal plan: **2,337 identical sample records**, 23 IK calls, one accepted candidate, 105 cache hits, 12 setup goals, 11 Cartesian solutions, 54 opening checks and the complete direct empty return. Joint values were identical; returned plans differed only in wall-time fields.

Instrumented monotonic driver time was **191.581541 seconds baseline versus 181.942557 seconds candidate**, a **5.0313%** reduction in this pair. OS/filesystem warming can favor the second process. Both included the same trace/telemetry overhead and fixed recorded parked-arm/head context, 156 ms after the initial planning event. This was the recorded Run38 900-second profile, not the current selected 1800-second profile. No evaluator pose, new fit, saved route or robot command entered the replay. The retained-send change was not exercised by this geometry-only comparison.

This bounded result does not predict live planning or mission speed, particularly with changing feedback, cache context and executor contention. Local full comparison/results/trace hashes are retained in the portable summary; large raw logs are not included in Git.

## Selected development profile and evidence boundary

The [profile guide](collision_quality_profile.md) retains the same explicit opt-in launch and [selected profile bytes](evidence/revision41/selected_runtime_profile.json): stock .017 m target, 5 mm lift, .35 m placement torso, .50 m clearance/.270 m release height, .60 m approach and .82 m bin standoff. Selected search/mission allowances are 1800/5400 wall seconds; ordinary defaults remain 420/270 seconds. Development observer/camera cap is 5700 seconds.

The software certificate and earlier nominal replay are preserved as their own evidence. The physical trial below used the unchanged selected profile; no result is reassigned to an earlier snapshot. The controller-change summary was written before the physical trial and retains that historical pending status.

## Completed Run40 physical result

Run40 (`11f08d9184fb`) completed pickup, extraction, compact transport, bin approach, opening and measured hand return. The [portable physical summary](evidence/run40/outcome_summary.json) binds the separately retained final observer reduction and phase reports. All 1,186 saved poses were valid. DONE was **656.600 ROS /2530.733186 observer wall seconds (42 min 10.733 s)**; the observer ended at 665.958 ROS. These wall values use the observer origin, while ROS numbers are absolute simulation stamps.

The final **35 identical book/bin poses**, 635.266–665.956 ROS, span 30.690 simulation seconds and 73.219398 observer wall seconds. All eight stock-book corners satisfy the audited core with zero tolerance: minimum side clearance **0.379252 mm**, minimum length clearance **80.020013 mm**, floor residual **+0.758599 micrometres**. Floor-support consistency has a separate 1 micrometre numerical allowance. The bin remained unchanged while the open hand moved away. This near-wall floor landing is not evidence for gentle maximum points or robust placement margins.

First exact book/bin contact was 631.144 ROS. Delivered inventory briefly omitted that same pair at 631.158 and reported it again at 631.160. These are two inventory episodes of one intended pair, not a physical impact count. No arm/tool–bin/table pair appears in the scoped inventory. The largest saved changed-message depth witness was **1.253744 mm**, not a full-run peak. Measured opening at 631.246 itself reported no physical-inside proof. Ten Cartesian return endpoints, arm HOME 649.082, torso HOME 656.190 and final measured hand return 656.438 establish the completed return separately.

Before motion, all **638 hold samples** from 388.140 to 608.762 ROS span 1369.441 wall seconds. Book-to-arm7 drift reached **13.771 mm /18.550 degrees**, with arm7-to-base displacement at most 27.900 micrometres. Actual corners protruded up to **47.642 mm beyond the logged padded nominal attachment**. That direct fixed-frame corner comparison uses evaluator poses only after the fact. Successful delivery does not validate a rigid attachment assumption or remove this planning-model gap.

The live plan accepted one candidate after 2,337 scene samples and 23 IK calls; total planning took 1371.804626 seconds, including 1304.586086 search seconds. This does not show a live speedup over Run39 or a causal performance result. Current software, profile and official physics remain unchanged.

The actual PLACE selection preserves all 91 atomic samples from 609.128 to 655.628 for the 608.904–656.438 event interval, maximum gap 0.948 seconds, without interpolation. The later [sampled geometry review](evidence/run40/actual_geometry_summary.json) found no overlap in **317 atomic screen states**, **316 actual-tool/book comparisons** and **129 table/bin states**. All six jobs completed once; their 36.020250-second execution cost is not live-planner performance. Separation was already established by world AABBs, so no narrow-phase calls were needed. The conservative minimum bounds include 7.165 mm arm/screen, 23.257 mm actual-book/screen, 32.734 mm PLACE visual-arm/table and 16.333 mm actual-tool/bin separation. They are not exact closest distances or physical-contact depths.

All available atomic rows in the five stated motion windows were selected. The sole actual-tool/book omission was INIT_STOW 122.040 at 36 ms joint/pose skew under the unchanged 20 ms gate; atomic arm/head and nominal-tool checks still included that row. Three unrecorded passive/base link poses received no nominal substitution. Maximum sample gaps were 0.972 seconds overall and 0.948 in PLACE; its requested endpoints have 224 ms and 810 ms gaps. The long planning hold and all between-sample motion are outside this audit. Official self-collision is disabled, the screen is a visual envelope, and fixed official table/evaluator transforms were used only retrospectively. This does not establish continuous or general collision safety or accurate nominal attachment during the hold.

Independent archive verification passed all **373 manifested files plus the manifest**, all 272 source files across archive/certificate/tar scopes, 64 installed modules, 11 data files and 40 mesh mappings. Saved official before/after checks each cover 895 files: 893 exact plus 2 permitted timestamp-header refreshes with unchanged payloads. The archive observation hash is 9b698746… and differs from the root analysis-journal hash. Saved shutdown evidence confirms the ten owned PIDs absent; this documentation does not query current processes. The portable outcome summary binds these separate evidence hashes. The earlier physical report retains its pre-geometry pending wording without byte changes.

Run40 repeats seed 101, column 2, red in the same development scenario as Run39. Neither delivery is a randomized five-trial reliability demonstration, a gentle-score judgment or a five-minute submission.
