# Revision51 validation and timed trial

Revision51 passed all **3,169 tests**, with zero failures, errors or skips, both selected/default four-node constructors, 895 official-file checks and 40 resolved-mesh checks. Full final16 validation finished on 12 September at 00:25:33 UTC in 256.956 seconds. The 303-file source tree is `6101fcee15b13e4b7efa6a067f905245b96da872cdb66c1a38651f1cafeb675e`; certificate SHA is `d4d7100546694d26e1cc9f3ce6c527a38fb8c4f5dab07e6e6d4226a97160dcd8`.

The exact tested package was promoted at 00:26:47 UTC, backing up all three replaced files before copying eight changed paths. Five files were added. Runtime installation and mesh resolution passed at 00:27:45 UTC. Official physics, the selected `590bc458…` profile, planned routes, command durations and force limits remain unchanged.

The force helper now specializes ordinary three-component float64 vectors using the same NumPy dot product and square root as the previous norm call. Original conversion, finite-result rejection, force summation order and nonstandard-shape handling remain. In one controlled 2,000-message comparison, every contact/history/force/epoch/latch/cancel state prefix matched. The specialized callback used 37.66% less thread CPU; with the new timing wrapper it used 33.11% less. This archived gripper-message component excludes DDS, executor contention and full live workload; it does not predict an end-to-end time saving.

Bounded diagnostics add actual worker/main native thread identities and cumulative callback CPU counters to existing command and placement-planning statuses. They create no new timer or publisher and do not wrap geometry predicates. Callback durations include completed callbacks only; clock, TF, action callbacks, executor dispatch and other threads remain unattributed. These counters do not measure GIL waiting.

Run44 (`bef7db4d5e17`, seed 101, column 2/red) started after fresh sensors passed at **00:28:29 UTC**. It used the normal four-node launch and unchanged official simulation, with the existing external observer and CPU ledger. PICK succeeded at 459.381 seconds and compact transport at 599.202 seconds. PLACE began at 742.480 seconds. Planning accepted its first candidate after 240.264 seconds, using 2,277 samples and 23 IK calls. Run43 used 324.159 seconds and 2,335 samples; the changed live inputs and measured-torso branch prevent an identical-workload comparison.

**Run44 failed before placement motion:** after the pre-motion retention check, `require_planned_place_height` raised `placement_measured_torso_changed`. The mission entered ABORTED at **985.646 seconds (16 min 25.646 s)**, with no gripper opening or delivery. The error combines several possible measurement failures; the exact torso-feedback cause is under investigation. Do not report this abort time as a completed mission. The previous successful run remains [Run43 on revision49](revision49_validation.md), at 20 min 41.724 s. The 15-minute target remains unmet.

The observer completed its terminal tail at 1,035.836 seconds /248.650 ROS seconds. All 303 solution files and official assets passed the closing integrity check. The CPU ledger closed explicitly, the immutable archive contains 410 evidence files (manifest `1a3faa2b9445b37ba0306cbee1e5d93a4562afa6bcf7c4c1c4c06477830522b0`), and all eleven owned simulation/evaluation processes were confirmed absent.

Final evaluation retained 482 physical samples, no scoped scene-contact pairs and zero bin displacement. The held book remained outside the bin. During 111 stationary planning-hold poses, book/arm7 drift reached 4.739 mm /4.098 degrees; the sample gap reached 0.386 ROS seconds. Four completed geometry windows contained 202 atomic screen/book/measured-tool queries and 31 eligible HEAD table/bin queries, with no overlap found. Minimum arm/screen separation was 7.165 mm. Three unavailable passive gripper links were omitted rather than replaced. Maximum joint/pose skew was 16 ms, and the largest screen-sample gap was 0.992 ROS seconds. PLACE and HOME were absent. These are sampled observations, not continuous collision or shelf-plane clearance proof.

Completed PLACE diagnostic intervals totaled 243.046 monotonic seconds and 114.131 worker-thread CPU seconds. The registered contact callback completed 39,020 calls using 25.203 CPU seconds; joint callbacks used 1.003 CPU seconds. These scopes exclude message deserialization, clock, TF, action/executor dispatch and other work, and they do not measure GIL waiting. A separate source review is examining those costs before further changes.

The recorded initial torso height was 0.34999999073867594 m, about 9.261 nm below the configured 0.35 m upper limit. External samples bracketing the failure do not identify the exact internal rejection branch. A default nominal-height fallback and an optional faster-arm admission remain outside the certified package pending integrated tests and a new physical trial; no measured-height threshold has been relaxed.

Revision50's failed final15 snapshot is preserved: 3,168 tests passed and one extracted-constructor test lacked the new diagnostic callback dependency. Both real constructor modes and official/source checks passed. Revision51 changes only that test fixture, importing and supplying the real callback wrapper while retaining all original guard assertions. All 90 affected tests passed before the fresh full final16 run. Runtime bytes are identical between revisions50 and51; failed final15 was not promoted or given a passing certificate.

The direct placement-setup candidate remains outside the runtime: it passed a full fixed-input plan but saved only 1.56% planning wall time while increasing requested setup duration by 1.2 ROS seconds. Optional faster arm timings and further tool-mesh reuse remain separate development candidates.
