# Revision52 development validation

Revision52 combines tool-mesh reuse, optional faster arm timing with velocity admission, and a return to the nominal torso-height route. The selected development profile uses `placement_transport_speed_scale: 1.25` and `measured_place_height_enabled: false`. Official models, controllers, friction and physics remain unchanged.

The frozen source has 318 files, tree `871f95bdbbcae2828ccaf28d631a0febc0b00608658c78863efe9d27427a9c7f`; selected profile SHA is `6ecc7345a8e130db70033061afe2232ca37ff657539841fd68a8d3f5d8871d2c`. Independent composition review verified the exact component sources and complete restoration of the preceding node. Full final17 validation passed **3,309 tests**, with zero failures, errors or skips, both selected/default four-node constructors, 895 official-file checks and 40 resolved-mesh checks. It finished at **01:16:56 UTC on 12 September** in 267.275 seconds. The certificate SHA is `a9f4670819821ed2d47bfdd10443f3a6728d3c68c96c637d0b84c47ab50aa14d`; candidate, snapshot and exported source inventories match exactly.

The exact validated package was promoted at **01:18:14 UTC**, backing up all 13 replaced files before copying 28 changed paths, including 15 additions. Runtime build, installation and asset readback passed at 01:18:55 UTC. Software validation does not establish delivery or mission speed.

Tool geometry now reuses immutable local mesh bytes while preserving world transforms, collision predicates, sample order and verdicts. In one fixed-context full-plan comparison, all 2,335 sampled arguments and verdicts matched, as did the route and command assembly. Planning wall time fell from 76.115 to 69.123 seconds (9.19%). The tool candidate passed 347 focused checks. This isolated comparison excludes live ROS and simulator workload and does not establish a mission time saving.

The motion component passed 212 focused checks, including 52 timing, 47 velocity-admission and 46 torso cases, plus existing retained-action races, lifecycle, placement ordering, contact and diagnostic cases. Selected supported compaction and registered placement/empty-return motions divide their serialized point times by 1.25. Each faster send checks fresh measured starting joints and the actual serialized segment slopes against the unchanged official URDF velocity limits before sending. The normal watchdogs remain. Shelf lift/withdrawal, torso, gripper, navigation and marker-search timing remain unchanged. Software checks do not establish physical retention, acceleration limits or tracking quality at the new speed.

The nominal torso route replaces the optional measured-height shortcut that aborted Run44 before placement motion. Its ordinary torso action, checked scene/support path and retention gates remain. It does not claim the shortcut's separate micrometre-level settled-height admission. The shortcut's thresholds are unchanged; Run44's exact internal feedback rejection was not recorded and remains uncertain.

A separate 20-second requested main-thread CPU profile passed 22 stdlib lifecycle checks on its original source, then all 22 again on the explicitly rebound R52/final17 successor. The normal Run45 launch and separate analysis helpers passed 47 and 29 checks. The installed entrypoint was verified, and a separately tested bootstrap bound the actual mission UUID before invoking the unchanged entrypoint through the diagnostic wrapper. Bootstrap waiting took 2.517 seconds. Profiling perturbs scheduling, so this trial is explicitly instrumented.

Run45 `2ca8e74e9f53` used the exact final17 package and unchanged official assets. Pickup succeeded at 466.097 seconds. Compaction passed final retention at 585.726 seconds; its command took 90.621 seconds, compared with 109.425 seconds in Run43. These are different live trials, not a matched timing comparison. PLACE began at 708.530 seconds. At 755.601 seconds (**12 min 35.601 s**) the mission aborted on `payload_hazard:contact_lost`, before placement motion, opening, delivery or return. This is not a successful mission time.

The main-thread profile started just before the abort and ran 20.006 wall seconds, recording 19.069 thread CPU seconds. Subscription taking and nested message construction were prominent: `_take_subscription` had 15.862 inclusive CPU seconds. Inclusive totals overlap; most of this window follows cancellation, and profiler overhead prevents using these numbers as normal mission timing or a causal GIL measurement. The measurements motivate investigating message decoding, not weakening contact checks.

Independent observer records contain both exact target/fingertip contact pairs across the abort, with a maximum 2 ms producer gap in the bounded review window. Bracketing sampled book translation was approximately 0.081 mm. These observations do not support a physical contact loss at the abort, but they cannot establish which messages the manipulation node consumed. Its internal contact timestamps and exact decision clock were not recorded, so the precise freshness failure remains unresolved. The closed journal contains 375 physical pose samples; the book remained outside the bin and no scoped scene-contact pair was recorded. Completed-motion geometry is audited separately; no PLACE or HOME motion is fabricated.

The completed-motion geometry audit ran four bounded jobs once in 14.210 seconds. It checked 186 atomic screen poses, including actual tool/book comparisons, and 24 eligible head/bin-approach poses against table and bin. All queried pairs were separated by their conservative bounds; no overlaps or narrow checks occurred. Minimum arm/screen separation was 7.165 mm and actual book/screen separation was 53.091 mm. Maximum pose/joint skew was 14 ms and maximum sample gap was 1.020 seconds. Three unavailable passive tool/base-finger link poses remain omitted. These are retrospective sampled checks, not continuous collision or shelf-clearance proof, and no PLACE/HOME motion was included.

The observer finished at 806.273 seconds. All 11 owned processes were subsequently stopped and verified absent. The immutable 445-file Run45 archive manifest is `cb2b7ff470afe97bb9d4c6599ba0fb95b1eb9d0b213816fca88237eb2a71f542`; official and solution source inventories matched after the run. The combined physical/failure/geometry summary SHA is `32f6a33972efa41e3a8c3cf835e7f682cab5248a0421a7de515000495a55770d`.

The latest successful full mission remains Run43 at **20 min 41.724 s**. Run44 aborted at **16 min 25.646 s** without placement. The 15-minute successful-mission target remains unmet.
