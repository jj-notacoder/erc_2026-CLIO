# Revision53 contact decoding validation

This candidate keeps the existing contact and placement safety callbacks and replaces generated ROS contact-message construction with an optional complete CDR1 decoder. The normal default remains `raw_contacts_enabled: false`; the selected development profile enables it. Contact topics, QoS, message order, force calculations, timestamps, histories, collision decisions and the R52 movement profile remain unchanged. Official models, controllers and physics are unchanged.

The decoder validates the complete serialized schema before exposing immutable field views. It retains all headers, entities, names, IDs, vector arrays, depths and both wrench bodies. Unsupported encodings or malformed data cancel the current work, latch a persistent wire fault, attempt the existing measured gripper hold, and reject subsequent normal commands. Stop/cancel commands remain available. No malformed message is converted into an apparently safe empty contact message.

The final focused sequence passed **94 tests**: 83 serializer/schema/state cases and 11 integration/admission cases. These include both byte orders, IEEE values, strings, malformed lengths, per-delivery callback state, empty frames, contact epochs, placement guards, normal subscription routing and permanent fault admission. The complete original contact callback and placement guard remain unchanged. An earlier test attempt stopped on a Python-version-dependent AST hash in the test fixture; it was corrected to an exact method-text hash without changing runtime code or behavioral assertions.

An isolated real ROS test published typed Contacts and received four raw messages over the exact sensor QoS on domain127. Empty and nonempty messages decoded correctly, with no retries or faults, and the node/executor were closed. This verifies the local ROS transport path, not live Gazebo mission performance.

The initial timing fixture included an extra test observer inside the callback. Its recorded 39.37% wall-time reduction therefore is not the selected stock-mode callback benchmark. That result is preserved. A single corrected comparison set the fine controller to `None` equally in both paths, matching selected retained-stock operation. For the same 384-message synthetic stream in typed/raw/raw/typed order, complete decode-plus-callback wall times were 0.123713/0.059608/0.056940/0.122453 seconds. Average raw wall time was 52.65% lower and thread CPU was 52.35% lower; every per-delivery state matched. Serialization, warmup, state comparison and context updates were outside the timing. This is an offline fixture measurement, not a live mission speedup.

The candidate is frozen at 324 files with tree `1fee829433dfea1610e9043b65730838c13be1e352abf9000706ef9dda670535`. Its nine overlay paths comprise three replacements and six additions; the complete package matches the focused test source. The selected profile SHA is `93b9376320fad44acee85ec41c271ee8bf210e5d9fbfe376b3adcd6759641dfb`.

Full final18 validation passed **3,403 tests**, with zero failures, errors or skips, both selected/default four-node constructors, 895 official-file checks and 40 resolved-mesh checks. It finished at **02:05:34 UTC on 12 September** in 267.343 seconds. Snapshot SHA is `ca218fd22018fe1353dac265526e1bc45d9a404614ed98722dc93d9f9486881f`; certificate SHA is `b7923d2148b74ab9abe065839c8a649a652e30be75ac2f2f705b02662be8e602`. Independent export verification confirmed identical candidate/snapshot/export inventories, all 41 evidence files and 1,209 unchanged complete snapshot entries. Actual constructor results confirm both selected contact subscriptions use raw delivery and the default contact subscription uses typed delivery.

The exact package was promoted at **02:07:21 UTC**, with all three replaced files backed up and verified before nine paths were copied. Runtime build and native module/mesh resolution passed at 02:07:47 UTC. The normal launch passed 47 checks and its analysis helpers passed 29 checks before binding. No profiling/bootstrap wrapper is included in this run.

Run46 `50e4c168c32f` completed the full mission at **981.073249 wall seconds (16 min 21.073 s)**, after sensor admission at 02:08:09 UTC. Pickup completed at 427.709117 seconds, COMPACT at 545.451821, placement planning at 823.864543, measured arm HOME at 963.283762, and the measured placement-hand return at 980.754844. The observer finished its tail at 1001.429478 seconds. The successful mission is 260.650711 seconds faster than Run43, leaving 81.073249 seconds to the requested 15-minute target. These are separate physical runs, not a matched benchmark isolating the decoder.

PLACE planning took **152.251978 seconds with 2,336 scene samples**, versus Run43's 324.158671 seconds and 2,335 samples. Official physics, controller/model bytes, sensor settings, stock 0.017 master target, shelf extraction timing and R52's optional 1.25 arm speed remain unchanged. Run46 used no cProfile or launch bootstrap wrapper.

The independent physical reduction contains 466 producer-stamped poses. All eight final book corners are inside the conservative bin core; the floor residual is +0.778298 micrometres under the unchanged analysis tolerance. Twenty-nine identical settled book/bin samples span 25.982 ROS seconds. Minimum side clearance is only **0.342695 mm**, so successful containment does not establish a robust or gentle landing. The bin pose did not move. The only scoped recorded scene pair is the intended book/bin contact; that report alone does not certify continuous collision clearance.

All six retrospective geometry jobs passed once in 28.153376 seconds. No overlap was found in 254 atomic screen/actual-tool/actual-book samples and 97 eligible table/bin poses. Minimum conservative separations were 7.165 mm arm/screen, 27.483 mm book/screen, 37.988 mm PLACE arm-visual/table and 13.958 mm measured-tool/bin. These are mesh AABB lower bounds; zero narrow checks were needed. All available samples in the requested motion windows were selected. Maximum joint/pose skew was 16 ms, with no skew omissions, and the maximum sample gap was 1.028 seconds. Three unavailable passive tool links remain omitted; nominal geometry does not substitute for their actual poses. This sampled result does not prove continuous or general self-collision safety. The combined outcome summary SHA is `a230d7953927ba09e7ed22f0ddb5998c6e2479e6b173627c5596ba38a86e887f`.

The archived run contains 437 files, manifest `2e8dd6da4f0b2922c5c0c701d54dd391a401a9b36326f42243ff5cf7b1c3b532`. Before/after checks verified all 324 solution files and official asset integrity. Observer/camera tails finished, the owned team and CPU ledger stopped, and all eleven recorded processes were verified absent after world shutdown. Closed journal SHA is `a3ddea599855a03d7a87074a05b9327d8a745312a92e50aa4a98977374a8f53a`; shutdown record SHA is `7f58fe953acfd28025108b9246870f5cb4188f07eb56b8264164dbcfaf885191`. No external repository push was performed for this revision. The 15-minute target and competition readiness remain unresolved.
