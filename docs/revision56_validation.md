# Revision56 software pass and Run49 pre-open failure

R56 / final21 passed **3,634 tests with zero failures, errors or skips**. The [validation certificate](../../output/official_reference/test_evidence/collision_quality_final_21/validation_certificate.json) covers the complete 334-file source snapshot, selected/default constructor checks and official asset checks. The [composition record](../../output/official_reference/collision_quality_candidate/revision56/staging_record.json) and [promotion record](../../output/official_reference/collision_quality_candidate/revision56/promotion/promotion.json) identify the exact installed revision. Software validation does not establish a physical delivery result.

**Run49 (`9f573d4597cf`) is closed and failed before opening at 829.234216601 observer wall seconds (13 min 49.234 s).** This is a stop duration, not a delivery time. The pre-open gate reported `measurement_timeout:stationary_closed_at_checked_endpoint`; no opening, release, return or measured HOME followed. The former immediate flag rejection was not reported, but the exact final veto is unknown. The book remained held above the bin at about z=1.02225 m, without whole-book containment or floor support. R56 is pending correction. See the [closed summary](../../output/official_reference/run49_review/completed_trial_summary.json), [terminal report](../../output/official_reference/run49_review/quality_evaluation/terminal_review01.md) and [evidence index](../../output/official_reference/run49_review/evidence_index.json).

R56 corrects the selected stock-close lifecycle that caused the [R55 / Run48 pre-open failure](revision55_validation.md). After stock-close admission and immediately before publishing a new close command, it clears the historical open-confirmed flag under the existing locking order. Clearing that flag invalidates an earlier opening proof; it does not assert successful closure or retention. Failed publication and failed close checks still cannot establish retention. The pre-open gate retains all three rejection conditions and now distinguishes an open-confirmed flag, disabled payload monitoring and an active retention probe. The old Run48 event did not log those internal flags, so the source explanation remains distinct from a reconstructed live snapshot.

The optional completed-frame search condition is **selected on for Run49 and defaults off**. It can choose the existing search-turn branch early after three consecutive, distinct, completed negative detector results for the current trial, target and view request. Producer timestamps, RGB/depth skew, sequence, per-frame expiry and freshness at consumption are checked. Positive marker-cloud handling takes priority; invalid, stale, missing or replayed results cannot authorize an early turn. The existing five-second fallback, waypoints, turn limit and abort rules remain. Three detector misses are evidence from completed frames, not proof that a physical marker is absent. The [marker source review](../../output/official_reference/run48_review/marker_revision02_independent_review/README.md) describes that scope.

The [selected profile](../../output/official_reference/collision_quality_candidate/revision56/runtime_profile.json) adds only the mission marker-search selection to R55's overrides. Arm timing scale 3.0, the pre-open stationary gate, stock close and raw contacts remain selected; measured torso-height selection remains disabled. Official physics and assets, shelf extraction timing, collision checks, measured velocity admission, retention checks, settle requirements and watchdog guards are retained. The [official source check](../../output/official_reference/test_evidence/collision_quality_final_21/official_source_check_after.json) and full validation evidence remain separate from closed physical outcome evidence.

Focused validation passed [311 stock/lifecycle cases](../../output/official_reference/run48_review/stock_lifecycle_revision02_focused_attempt01/result.json) and [92 completed-frame cases](../../output/official_reference/run48_review/marker_negative_focused_attempt01/result.json). The [original stock attempt](../../output/official_reference/run48_review/stock_lifecycle_focused_attempt01/result.json) is preserved: 308 passed and two positive-path fixtures failed because they omitted the selected delivery-evidence collection flag, leaving the real callback's raw buffers empty. The corrected fixture uses the selected flag and actual joint/odometry callbacks, strengthens sample assertions and retains a disabled-collection negative case. Production bytes were unchanged by that fixture correction. These focused counts are separate runs, not additional tests to add to the full-suite total.

**The verified fallback remains [R54 / Run47](revision54_validation.md): 898.556826535 seconds (14 min 58.557 s).** That is one successful trial with only 1.443 seconds of time headroom and 1.806 mm final side clearance. Its landing rotation, transient sampled floor excursion and sampled rather than continuous geometry limits remain part of the proof. The [Run48 failure record](../../output/official_reference/run48_review/completed_trial_summary.json) is also preserved. Neither record is replaced or extended by the failed Run49 trial. No software rollback is implied.

The two unsuccessful search waits totaled **1.081960 wall seconds** in Run49, versus 22.777724 in Run47 and 22.744722 in Run48. The final positive view took 0.231384 seconds. These saved phase windows show shorter search waits; different trial inputs and the incomplete delivery prevent attributing all mission-time differences to the feature. The exact endpoints and distinct ROS timing fields remain in the terminal report.

All eleven owned processes are absent and the 443-file archive is closed. The observer ended at 878.299305198 wall seconds; the ledger retained 94 samples over 938.1486 wall seconds. All 12 grasp captures and six bin frames were saved, with 30 bin-camera TF extrapolation errors disclosed. The prepared six-job geometry audit remains unrun; no new no-overlap or continuous-clearance claim is made.
